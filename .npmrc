# Registry routing configured per-workflow:
# - Alpha releases: GitHub Package Registry (via NPM_CONFIG_REGISTRY in publish-alpha.yml)
# - Production releases: npm public registry (default, configured in release.yml)

# GitHub Package Registry authentication (used by alpha publishing workflow)
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
